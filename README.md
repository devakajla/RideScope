# RideScope

Bike ride analysis and prediction platform built with FastAPI + React.

## Setup

1. Clone the repo
2. `cp .env.example .env`
3. Backend: `cd backend && pip install -r requirements.txt && uvicorn main:app --reload`
4. Frontend: `cd frontend && npm install && npm start`
